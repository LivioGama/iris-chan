{
  "name": "iris-v2",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "cd packages/core && bun run dev",
    "start": "cd packages/core && bun run start",
    "build": "bun run --filter '*' build"
  }
}
